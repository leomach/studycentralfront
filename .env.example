# URL base do backend Go (CLAUDE.md §8). Ex.: http://localhost:8080
# Se ficar VAZIO, o app usa o mock provisório em lib/api/mocks/ (§8) para
# permitir desenvolvimento sem o backend.
NEXT_PUBLIC_API_URL=
