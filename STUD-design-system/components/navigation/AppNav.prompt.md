`AppNav` sits at the top of a screen, inside the canvas so it inherits the canvas colour.
Navigation itself lives in `TabBar` at the bottom.

```jsx
<AppNav title="Questões" pending={2} action={<Button variant="light" size="sm">Filtros</Button>} />
```
